# LiteAPI keys (Phase 2: guest = B2C, logged-in = CUG)
# Copy to .env.local and fill with real values. Never commit .env.local.
# These are server-only and never exposed to the browser.

# Both required; no fallback — app uses these for guest vs logged-in.
LITEAPI_KEY_B2C=    # Public/B2C key — used for guests (not logged in)
LITEAPI_KEY_CUG=    # CUG key — used for logged-in users

# LiteAPI payment SDK: use "live" or "sandbox" (must match your LiteAPI key environment).
# Do not use your own Stripe publishable key here — LiteAPI creates the PaymentIntent on their Stripe account.
# NEXT_PUBLIC_LITEAPI_ENV=live

# Optional
# NEXT_PUBLIC_LITEAPI_ENV=live

# (Deprecated) Rates search streaming — unused; app uses non-stream search only. See docs/deprecated/STREAM_SEARCH_README.md.
# USE_RATES_STREAM=true
